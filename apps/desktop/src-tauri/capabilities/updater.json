{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "updater",
  "description": "Allow update checks only from normal AtrisShot UI windows",
  "windows": ["main", "editor"],
  "permissions": ["updater:default"]
}
