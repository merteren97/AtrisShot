{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-drag",
  "description": "Allow the result overlay to drag saved screenshots to other applications",
  "windows": ["overlay"],
  "permissions": ["drag:allow-start-drag"]
}
