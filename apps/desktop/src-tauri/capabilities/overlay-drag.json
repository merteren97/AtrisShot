{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "overlay-drag",
  "description": "Allow only the result overlay to drag saved screenshots and move its window",
  "windows": ["overlay"],
  "permissions": [
    "core:window:allow-start-dragging",
    "drag:allow-start-drag"
  ]
}
